path = new Path.Circle(100, 100, 30);
path.fillColor = "white";

var circleArr = [];
circleArr.push(path);
console.log(circleArr[0]);

function onFrame() {
    for (i = 0; i < circleArr.length; i++)
}